DATABASE_URL=postgresql://user:password@localhost:5432/workspace_db?sslmode=disable
# RSA public key from the auth service (PEM format, use \n for newlines in .env files)
# Example: JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nMIIBIjAN...\n-----END PUBLIC KEY-----
JWT_PUBLIC_KEY=
PORT=3002
